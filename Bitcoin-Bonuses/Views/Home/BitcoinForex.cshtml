@model Bitcoin_Bonuses.Models.BitcoinTradingDB
@{
    ViewBag.Title = "Bitcoin Forex Bonuses";
}


<div class="tablewrap">
    <div class="boxHeader">@ViewBag.Title</div>
    <div class="tablewrapInternal">
        <table class="bonus">
            <thead class="bonus">
                <tr>
                    <th>Country</th>
                    <th>Site Banner</th>
                    <th>Site</th>
                    <th>Bonus</th>
                    <th>Claim Now</th>
                    <th>More Info</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Bitcoin)
                {
                    if (item.BonusType == "Forex")
                    {
                        string row = "even";
                        if ((item.RowID) % 2 == 1)
                        {
                            row = "odd";
                        }
                    <tr class="@row">
                        <td>
                            <img src="@item.FlagIcon" alt="@item.FlagIconAlt" width="40" height="20"/>
                        </td>
                        <td>
                            <img src="@item.Image"alt="@item.ImageAlt" />
                            @*100x33 is the usual size*@
                        </td>
                        <td>
                            <a href="@item.ClaimNowURL" target="_blank">@item.BonusName</a>
                        </td>
                        <td>
                            @item.BonusAmt @item.MoreInfo
                        </td>
                        <td>
                            <a href="@item.ClaimNowURL" class="claimMe" target="_blank">Claim Now!</a>
                        </td>
                        <td>
                            @Html.ActionLink("More Info", "_FeaturedDisplayPartial", new { id = item.Id }, new { @class = "moreInfo" })
                        </td>
                    </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
<div class="contentbox">
    <div class="boxHeader">Forex with Bitcoins</div>
    <div class="contentboxInternal">
        <p>
            Bitcoins have made their way into Forex Trading. This means that Bitcoin currency is used in pair with another country's currency along with the other 
        currency pairs. The bonus returns on these vary depending on the provider, however usually it will be a guarantee a 100% return until the investor gains. Forex trading 
        involves an investor determining whether a currency will rise or fall in a set amount of time. Answering correctly will put that investor at a gain, however incorrect 
        answers will put that investor at a loss.
        </p>
    </div>
</div>

@*@section featured{
    <div class="fullwidthbox">
        <div class="fullwidthinternal">
            <p>test</p>
        </div>
    </div>
}*@